{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "packages": {
    "node": "22"
  },
  "steps": {
    "build": {
      "commands": [
        "...",
        "npm --prefix frontend ci --include=dev",
        "npm --prefix frontend run build"
      ]
    }
  }
}
